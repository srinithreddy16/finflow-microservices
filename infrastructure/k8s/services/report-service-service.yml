apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: finflow
  labels:
    app: report-service
spec:
  type: ClusterIP
  selector:
    app: report-service
  ports:
    - name: http
      port: 8089
      targetPort: 8089
      protocol: TCP
