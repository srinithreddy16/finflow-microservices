apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: finflow
  labels:
    app: account-service
spec:
  type: ClusterIP
  selector:
    app: account-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
