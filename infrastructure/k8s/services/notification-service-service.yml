apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: finflow
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8087
      targetPort: 8087
      protocol: TCP
